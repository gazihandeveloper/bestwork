# BestWork eshop (kök site) — örnek env (gerçek sır içermez)
# Kopyala: cp eshop/.env.example eshop/.env.local
#
# Backend Gin router '/api' grubu altında olduğu için base URL '/api' sonekini içerir.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8090/api
